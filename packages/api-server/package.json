{
  "name": "@curvefi/api-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint --cache --cache-location ../../.cache/eslint/api-server --cache-strategy content .",
    "lint:fix": "eslint --cache --cache-location ../../.cache/eslint/api-server --cache-strategy content . --fix",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "fastify": "5.12.1"
  }
}
