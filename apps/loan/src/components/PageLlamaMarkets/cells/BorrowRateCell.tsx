import { PoolFromApi } from '@/entities/pools'
import { useLendingSnapshots } from '@/entities/lending'

export const BorrowRateCell = ({ data }: { data: PoolFromApi }) => {
  // const { data } = useLendingSnapshots({ chainId: data.blockchainId, poolId: data.id })
  return null
}
