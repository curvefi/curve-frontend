{
  "name": "tests",
  "version": "0.1.0",
  "description": "Curve DApp tests",
  "main": "index.js",
  "scripts": {
    "run:nodes": "node runHardhatNodes.js",
    "kill:nodes": "node killHardhatNodes.js",
    "cy:open:e2e:main": "yarn run:nodes && CYPRESS_CONFIG=main cypress open --browser chrome --e2e && yarn kill:nodes",
    "cy:open:e2e:lend": "yarn run:nodes && CYPRESS_CONFIG=lend cypress open --browser chrome --e2e && yarn kill:nodes",
    "cy:open:e2e:loan": "yarn run:nodes && CYPRESS_CONFIG=loan cypress open --browser chrome --e2e && yarn kill:nodes",
    "cy:run:e2e:main": "yarn run:nodes && CYPRESS_CONFIG=main cypress run --browser chrome --e2e && yarn kill:nodes",
    "cy:run:e2e:lend": "yarn run:nodes && CYPRESS_CONFIG=lend cypress run --browser chrome --e2e && yarn kill:nodes",
    "cy:run:e2e:loan": "yarn run:nodes && CYPRESS_CONFIG=loan cypress run --browser chrome --e2e && yarn kill:nodes"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.6",
    "@nomicfoundation/hardhat-ethers": "^3.0.6",
    "@types/cypress": "^1.1.3",
    "@types/wait-on": "^5.3.4",
    "cypress": "^13.9.0",
    "dotenv-flow": "^4.1.0",
    "ethers": "^6.12.1",
    "hardhat": "^2.22.4",
    "wait-on": "^7.2.0"
  }
}
